<UserControl x:Class="TapWeaver.UI.Views.RecorderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Text="Macro Recorder" FontSize="22" FontWeight="Bold"
                   Foreground="#212121" Margin="0,0,0,16"/>

        <!-- Controls Card -->
        <Border Grid.Row="1" Style="{StaticResource CardStyle}" Margin="0,0,0,16">
            <StackPanel>
                <TextBlock Text="Use the buttons below or configure a global hotkey to start/stop recording. All keyboard and mouse events will be captured and converted to a macro."
                           TextWrapping="Wrap" Foreground="#757575" Margin="0,0,0,16" FontSize="13"/>

                <!-- Status Indicator -->
                <Border CornerRadius="4" Padding="12,8" Margin="0,0,0,16" HorizontalAlignment="Left">
                    <Border.Style>
                        <Style TargetType="Border">
                            <Setter Property="Background" Value="#E8F5E9"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsRecording}" Value="True">
                                    <Setter Property="Background" Value="#FFEBEE"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <Ellipse Width="10" Height="10" Margin="0,0,8,0">
                            <Ellipse.Style>
                                <Style TargetType="Ellipse">
                                    <Setter Property="Fill" Value="#4CAF50"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsRecording}" Value="True">
                                            <Setter Property="Fill" Value="#F44336"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Ellipse.Style>
                        </Ellipse>
                        <TextBlock FontWeight="SemiBold" FontSize="13">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Text" Value="Ready to Record"/>
                                    <Setter Property="Foreground" Value="#4CAF50"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsRecording}" Value="True">
                                            <Setter Property="Text" Value="Recording..."/>
                                            <Setter Property="Foreground" Value="#F44336"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </StackPanel>
                </Border>

                <!-- Buttons -->
                <StackPanel Orientation="Horizontal">
                    <Button Content="Start Recording" Style="{StaticResource DangerButtonStyle}"
                            Command="{Binding StartRecordingCommand}"
                            MinWidth="160" Height="44" Margin="0,0,12,0" FontSize="14"/>
                    <Button Content="Stop and Use Macro" Style="{StaticResource SuccessButtonStyle}"
                            Command="{Binding StopRecordingCommand}"
                            MinWidth="160" Height="44" FontSize="14"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Recorded Steps -->
        <Border Grid.Row="2" Style="{StaticResource CardStyle}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12">
                    <TextBlock Text="Recorded Events" FontSize="15" FontWeight="SemiBold" VerticalAlignment="Center"/>
                    <TextBlock Text="{Binding RecordedSteps.Count, StringFormat=' ({0} steps)'}"
                               FontSize="13" Foreground="#757575" VerticalAlignment="Center"/>
                </StackPanel>
                <ListView Grid.Row="1" ItemsSource="{Binding RecordedSteps}"
                          BorderThickness="0" Background="Transparent">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="Type" Width="120"
                                            DisplayMemberBinding="{Binding Type}"/>
                            <GridViewColumn Header="Key/Button" Width="120"
                                            DisplayMemberBinding="{Binding Key}"/>
                            <GridViewColumn Header="Delay (ms)" Width="100"
                                            DisplayMemberBinding="{Binding DelayMs}"/>
                            <GridViewColumn Header="Description" Width="300"
                                            DisplayMemberBinding="{Binding Description}"/>
                        </GridView>
                    </ListView.View>
                </ListView>
            </Grid>
        </Border>
    </Grid>
</UserControl>
