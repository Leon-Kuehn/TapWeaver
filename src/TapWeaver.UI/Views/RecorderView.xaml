<UserControl x:Class="TapWeaver.UI.Views.RecorderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:svg="clr-namespace:SharpVectors.Converters;assembly=SharpVectors.Converters.Wpf">
    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Text="Macro Recorder" FontSize="20" FontWeight="Bold"
               Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,12"/>

        <!-- Controls Card -->
        <Border Grid.Row="1" Style="{StaticResource CardStyle}" Margin="0,0,0,16">
            <StackPanel>
                <TextBlock Text="Use the buttons below or configure a global hotkey to start/stop recording. Capture behavior can be adjusted in Settings."
                           TextWrapping="Wrap" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,16" FontSize="13"/>

                <!-- Status Indicator -->
                <Border CornerRadius="4" Padding="12,8" Margin="0,0,0,16" HorizontalAlignment="Left">
                    <Border.Style>
                        <Style TargetType="Border">
                            <Setter Property="Background" Value="#E8F5E9"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsRecording}" Value="True">
                                    <Setter Property="Background" Value="#FFEBEE"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <Ellipse Width="10" Height="10" Margin="0,0,8,0">
                            <Ellipse.Style>
                                <Style TargetType="Ellipse">
                                    <Setter Property="Fill" Value="#4CAF50"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsRecording}" Value="True">
                                            <Setter Property="Fill" Value="#F44336"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Ellipse.Style>
                        </Ellipse>
                        <TextBlock FontWeight="SemiBold" FontSize="13">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Text" Value="Ready to Record"/>
                                    <Setter Property="Foreground" Value="#4CAF50"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsRecording}" Value="True">
                                            <Setter Property="Text" Value="Recording..."/>
                                            <Setter Property="Foreground" Value="#F44336"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </StackPanel>
                </Border>

                <!-- Buttons -->
                <WrapPanel>
                    <Button Style="{StaticResource DangerButtonStyle}"
                            Command="{Binding StartRecordingCommand}"
                            MinWidth="178" Height="44" Margin="0,0,12,0" FontSize="14"
                            ToolTip="Start capturing keyboard and mouse actions">
                        <StackPanel Orientation="Horizontal">
                            <svg:SvgViewbox Width="14" Height="14" Source="/Assets/Icons/record.svg" Margin="0,0,6,0"/>
                            <TextBlock Text="Start Recording" Style="{StaticResource HideInCompactModeTextStyle}"/>
                        </StackPanel>
                    </Button>
                    <Button Style="{StaticResource SuccessButtonStyle}"
                            Command="{Binding StopRecordingCommand}"
                            MinWidth="178" Height="44" FontSize="14"
                            ToolTip="Stop recording and send result to Sequencer">
                        <StackPanel Orientation="Horizontal">
                            <svg:SvgViewbox Width="14" Height="14" Source="/Assets/Icons/stop.svg" Margin="0,0,6,0"/>
                            <TextBlock Text="Stop and Use" Style="{StaticResource HideInCompactModeTextStyle}"/>
                        </StackPanel>
                    </Button>
                </WrapPanel>
            </StackPanel>
        </Border>

        <!-- Recorded Steps -->
        <Border Grid.Row="2" Style="{StaticResource CardStyle}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12">
                    <TextBlock Text="Recorded Events" FontSize="15" FontWeight="SemiBold" VerticalAlignment="Center" Foreground="{StaticResource TextPrimaryBrush}"/>
                    <TextBlock Text="{Binding RecordedSteps.Count, StringFormat=' ({0} steps)'}"
                               FontSize="13" Foreground="{StaticResource TextSecondaryBrush}" VerticalAlignment="Center"/>
                </StackPanel>
                <ListView Grid.Row="1" ItemsSource="{Binding RecordedSteps}"
                                                    Style="{StaticResource ModernListViewStyle}"
                          HorizontalContentAlignment="Stretch"
                                                    Margin="0,2,0,0"
                          ScrollViewer.HorizontalScrollBarVisibility="Auto">
                    <ListView.View>
                                                <GridView ColumnHeaderContainerStyle="{StaticResource ModernGridViewHeaderStyle}">
                            <GridViewColumn Header="Type" Width="120"
                                            DisplayMemberBinding="{Binding Type}"/>
                            <GridViewColumn Header="Key/Button" Width="120"
                                            DisplayMemberBinding="{Binding Key}"/>
                            <GridViewColumn Header="Delay (ms)" Width="100"
                                            DisplayMemberBinding="{Binding DelayMs}"/>
                            <GridViewColumn Header="Description" Width="300"
                                            DisplayMemberBinding="{Binding Description}"/>
                        </GridView>
                    </ListView.View>
                </ListView>
            </Grid>
        </Border>
    </Grid>
    </ScrollViewer>
</UserControl>
